// Superseded by app/assignments/page.tsx — kept as an empty re-export so any
// remaining imports don't break the build during migration.
export default function AssignmentDashboard() { return null }
