{
  "name": "virtualcoach-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "powershell -ExecutionPolicy Bypass -File ./start-all.ps1",
    "stop": "powershell -ExecutionPolicy Bypass -Command \"8080, 8081, 3000, 8083 | ForEach-Object { $p = Get-NetTCPConnection -LocalPort $_ -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique | Where-Object { $_ -gt 4 }; if ($p) { Stop-Process -Id $p -Force } }\"",
    "clean": "npm run stop"
  }
}
